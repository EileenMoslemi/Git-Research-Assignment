# Pull

A **pull** updates your local repository with the latest changes from a remote repository.
It downloads new commits and merges them into your current branch.

## What Happens When You Pull
1. Git fetches changes from GitHub
2. Git merges those changes into your branch

## Example Command
```bash
git pull origin main
```
## Source
https://git-scm.com/docs/git-pull
